<p id="notice"><%= notice %></p>

<h3>Tasks</h3>
<%= render "shared/tasks", tasks: @pending, status: true, button: 'Done' %>

<h3>Completed</h3>
<%= render "shared/tasks", tasks: @completed, status: false, button: 'Undo' %>

